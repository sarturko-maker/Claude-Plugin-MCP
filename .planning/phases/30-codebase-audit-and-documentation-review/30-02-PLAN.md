---
phase: 30-codebase-audit-and-documentation-review
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - README.md
  - DISCLAIMER.md
  - SECURITY.md
  - CONTRIBUTING.md
  - PRIVACY.md
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03, DOC-04, DOC-05, AUD-05, AUD-06]

must_haves:
  truths:
    - "README has POC banner, accurate install instructions (git clone + pip install + claude --plugin-dir), architecture overview, and usage examples"
    - "Every claim in README is verified against actual code (tool count, features, commands)"
    - "DISCLAIMER.md covers POC status, AI-assisted development, not legal advice, no liability"
    - "SECURITY.md has correct tool count (11), supported versions (2.0.x), accurate data flows"
    - "CONTRIBUTING.md and PRIVACY.md link to Claude-Plugin-MCP repo"
    - ".mcp.json local path documented in README with user setup note"
  artifacts:
    - path: "README.md"
      provides: "Accurate user-facing documentation"
      contains: "pip install"
    - path: "SECURITY.md"
      provides: "Security documentation with correct tool count"
      contains: "11"
    - path: "CONTRIBUTING.md"
      provides: "Contribution guide with correct repo links"
      contains: "Claude-Plugin-MCP"
    - path: "PRIVACY.md"
      provides: "Privacy policy with correct repo links"
      contains: "Claude-Plugin-MCP"
    - path: "DISCLAIMER.md"
      provides: "POC and liability disclaimer"
      contains: "not legal advice"
  key_links:
    - from: "README.md"
      to: "pyproject.toml"
      via: "install instructions reference pip install"
      pattern: "pip install"
    - from: "README.md"
      to: "scripts/start-mcp.sh"
      via: "setup instructions"
      pattern: "start-mcp"
    - from: "SECURITY.md"
      to: "src/"
      via: "tool count and ToolAnnotations table"
      pattern: "11.*tools"
---

<objective>
Update all documentation files to be accurate, cross-checked against actual code, and pointing to the correct repository (Claude-Plugin-MCP).

Purpose: Documentation must be truthful and publication-ready. Every claim must be verified against the codebase as it exists after Plan 01 bundling.
Output: Accurate README, DISCLAIMER, SECURITY, CONTRIBUTING, and PRIVACY files.
</objective>

<execution_context>
@/home/sarturko/.claude/get-shit-done/workflows/execute-plan.md
@/home/sarturko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-codebase-audit-and-documentation-review/30-CONTEXT.md
@.planning/phases/30-codebase-audit-and-documentation-review/30-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md with accurate install and usage</name>
  <files>README.md</files>
  <action>
Read the current README.md, then rewrite it with these sections in order:

1. **POC Disclaimer Banner** at the very top: Clear warning that this is a proof-of-concept, AI-assisted, not production-ready, not legal advice.

2. **Quick Start** code block:
   ```bash
   git clone https://github.com/sarturko-maker/Claude-Plugin-MCP.git
   cd Claude-Plugin-MCP
   pip install .
   claude --plugin-dir ./
   ```

3. **Prerequisites** (one line each, verified):
   - Python 3.11+ (required by Adeu dependency)
   - pip (for installing dependencies)
   - git (for cloning and for adeu git dependency)
   - Claude Code (Anthropic's CLI)

4. **What It Does** -- brief description of contract negotiation capabilities. Keep the existing good content about surgical diffs, multi-round negotiation, word-level diff, Track Changes output.

5. **Commands**:
   - `/negotiate` as the primary command with description
   - `/yolo-negotiation` mentioned as variant for experienced users
   - Brief description of what each does

6. **Architecture Overview**: Plugin structure (.claude-plugin/, skills/, commands/, src/, defaults/, scripts/), MCP server entry point, three-level config fallback.

7. **Usage Examples**: Keep existing examples for /negotiate and /yolo-negotiation if they are accurate. Cross-check each example against actual command files in commands/ directory.

8. **.mcp.json Setup Note** (DOC-05): Explain that .mcp.json uses ${CLAUDE_PLUGIN_ROOT} which Claude Code resolves automatically when using --plugin-dir. No manual path editing needed.

9. **License, Links** footer.

**Cross-checking (DOC-02):** Before finalizing, verify every claim:
- Count @mcp.tool decorators in src/ to confirm tool count (should be 11)
- Check commands/ directory for actual command names
- Check defaults/ for actual default files
- Verify the pip install command works with pyproject.toml structure
- Do NOT mention Cowork instructions (deferred per user decision)
  </action>
  <verify>
    <automated>grep -q "proof-of-concept\|POC\|Proof of Concept" README.md && grep -q "pip install" README.md && grep -q "/negotiate" README.md && grep -q "/yolo-negotiation" README.md && grep -q "Claude-Plugin-MCP" README.md && grep -q "3.11" README.md && grep -q "mcp.json" README.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>README has POC banner, quick start with pip install, prerequisites, commands, architecture, usage examples, .mcp.json note, all claims cross-checked against code</done>
</task>

<task type="auto">
  <name>Task 2: Fix SECURITY.md, CONTRIBUTING.md, PRIVACY.md, and verify DISCLAIMER.md</name>
  <files>SECURITY.md, CONTRIBUTING.md, PRIVACY.md, DISCLAIMER.md</files>
  <action>
1. **SECURITY.md** (AUD-05):
   - Read current SECURITY.md
   - Update "Supported Versions" table: change to 2.0.x supported, mark older versions as unsupported
   - Update tool count from 9 to 11 everywhere it appears (2 ingest + 5 action + 1 pipeline + 1 redline + 2 styler = 11)
   - Verify the ToolAnnotations table against actual @mcp.tool decorators in src/ -- keep source file references (they are now valid since src/ is bundled)
   - Update data flow descriptions to remain accurate
   - Update dependency list if pyproject.toml has different deps than what SECURITY.md lists
   - Ensure stateless processing and no document retention claims are accurate

2. **CONTRIBUTING.md** (AUD-06):
   - Read current file
   - Change ALL repository links from Claude-Plugin to Claude-Plugin-MCP
   - Issues and PRs should point to https://github.com/sarturko-maker/Claude-Plugin-MCP
   - Verify no other stale references

3. **PRIVACY.md** (AUD-06):
   - Read current file
   - Change ALL repository links from Claude-Plugin to Claude-Plugin-MCP
   - Verify no other stale references

4. **DISCLAIMER.md** (DOC-03):
   - Read current file
   - Verify it covers: POC status, AI-assisted development, not legal advice, not for production, all output AI-generated, must be reviewed by human, no liability
   - If any of these are missing, add them. If all present, leave as-is.
  </action>
  <verify>
    <automated>grep -q "2.0.x" SECURITY.md && grep -c "11" SECURITY.md | grep -qv "^0$" && ! grep -q "github.com/sarturko-maker/Claude-Plugin[^-]" CONTRIBUTING.md && ! grep -q "github.com/sarturko-maker/Claude-Plugin[^-]" PRIVACY.md && grep -qi "not legal advice\|no legal advice\|not.*legal.*advice" DISCLAIMER.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>SECURITY.md has 11 tools and 2.0.x versions, CONTRIBUTING.md and PRIVACY.md link to Claude-Plugin-MCP, DISCLAIMER.md covers all required disclaimers</done>
</task>

</tasks>

<verification>
- `grep -c "Claude-Plugin[^-M]" CONTRIBUTING.md PRIVACY.md SECURITY.md` returns 0 for each (no old repo links)
- `grep "11" SECURITY.md` shows correct tool count
- `grep "2.0" SECURITY.md` shows correct version
- README.md mentions pip install, python 3.11+, /negotiate, /yolo-negotiation, POC banner, .mcp.json
</verification>

<success_criteria>
- README is accurate, has POC banner, quick start, prerequisites, commands, architecture, .mcp.json note
- Every README claim cross-checked against actual code
- SECURITY.md has 11 tools, 2.0.x versions, accurate ToolAnnotations
- CONTRIBUTING.md and PRIVACY.md link to Claude-Plugin-MCP
- DISCLAIMER.md covers POC, AI-assisted, not legal advice, no liability
- No old repo links (Claude-Plugin without -MCP) in any doc
</success_criteria>

<output>
After completion, create `.planning/phases/30-codebase-audit-and-documentation-review/30-02-SUMMARY.md`
</output>
