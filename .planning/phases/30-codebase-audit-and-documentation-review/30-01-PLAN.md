---
phase: 30-codebase-audit-and-documentation-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - claude-v-claude/
  - .claude/settings.local.json
  - .gitignore
  - .claude-plugin/plugin.json
  - scripts/start-mcp.sh
  - .mcp.json
  - src/
  - pyproject.toml
autonomous: true
requirements: [AUD-01, AUD-02, AUD-04, AUD-07, AUD-08]

must_haves:
  truths:
    - "No stale files exist in the repo (claude-v-claude/ gone, settings.local.json gone)"
    - "plugin.json has correct version, repository URL, and privacy_policy field"
    - "start-mcp.sh auto-detects paths relative to its own location and checks Python 3.11+"
    - ".gitignore excludes .planning/, .venv/, __pycache__/, *.pyc, output/"
    - "No secrets, hardcoded personal paths, or unnecessary data in any config file"
    - "src/ directory with 78 Python files is bundled in the repo"
    - "pyproject.toml exists with correct dependencies including adeu git dep"
  artifacts:
    - path: ".gitignore"
      provides: "Correct exclusion patterns"
      contains: ".planning/"
    - path: ".claude-plugin/plugin.json"
      provides: "Plugin metadata with correct URLs"
      contains: "Claude-Plugin-MCP"
    - path: "scripts/start-mcp.sh"
      provides: "Auto-detecting MCP launcher"
      contains: "python3"
    - path: "pyproject.toml"
      provides: "Pip-installable package definition"
      contains: "adeu"
    - path: "src/__init__.py"
      provides: "Bundled MCP server source"
  key_links:
    - from: "scripts/start-mcp.sh"
      to: "src/mcp_server"
      via: "python -m src.mcp_server"
      pattern: "python3.*-m src.mcp_server"
    - from: ".mcp.json"
      to: "scripts/start-mcp.sh"
      via: "CLAUDE_PLUGIN_ROOT variable"
      pattern: "start-mcp.sh"
    - from: "pyproject.toml"
      to: "src/"
      via: "package definition"
      pattern: "packages.*src"
---

<objective>
Remove stale files, bundle the MCP server source, create pyproject.toml, and fix all configuration files so the repo is self-contained and correct.

Purpose: The repo must be a complete, installable plugin with no hardcoded paths, stale artifacts, or missing dependencies.
Output: Clean repo with bundled src/, pyproject.toml, fixed configs, no stale files.
</objective>

<execution_context>
@/home/sarturko/.claude/get-shit-done/workflows/execute-plan.md
@/home/sarturko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-codebase-audit-and-documentation-review/30-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove stale files and fix configuration</name>
  <files>claude-v-claude/, .claude/settings.local.json, .gitignore, .claude-plugin/plugin.json</files>
  <action>
1. Delete the entire claude-v-claude/ directory (4 stale test docx files).
2. Delete .claude/settings.local.json (GSD permissions -- not for distribution).
3. Update .gitignore to add these entries (keep all existing entries):
   - .planning/
   - Remove the comment "# .planning/ is intentionally tracked" and replace with the .planning/ exclusion
4. Update .claude-plugin/plugin.json:
   - Change "version" from "1.8.0" to "2.0.0"
   - Change "repository" from "https://github.com/sarturko-maker/Claude-Plugin" to "https://github.com/sarturko-maker/Claude-Plugin-MCP"
   - Add "privacy_policy" field pointing to "https://github.com/sarturko-maker/Claude-Plugin-MCP/blob/main/PRIVACY.md"
5. Scan all remaining files for hardcoded paths containing "/home/sarturko/" -- flag any found (start-mcp.sh will be fixed in Task 2).

Do NOT remove .claude/ directory itself -- only settings.local.json inside it (the directory may be needed for plugin structure).
  </action>
  <verify>
    <automated>test ! -d claude-v-claude && test ! -f .claude/settings.local.json && grep -q ".planning/" .gitignore && grep -q "Claude-Plugin-MCP" .claude-plugin/plugin.json && grep -q "privacy_policy" .claude-plugin/plugin.json && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>claude-v-claude/ deleted, settings.local.json deleted, .gitignore has .planning/ exclusion, plugin.json has correct repo URL + privacy_policy field + version 2.0.0</done>
</task>

<task type="auto">
  <name>Task 2: Bundle MCP server source and create pyproject.toml</name>
  <files>src/, pyproject.toml, scripts/start-mcp.sh</files>
  <action>
1. Copy the entire src/ directory from ~/Claude-Plugin/src/ into this repo at src/. This is 78 Python files across subdirectories (config, ingestion, mcp_server, models, negotiation, orchestration, pipeline, validation). Do NOT copy __pycache__/ directories.

2. Create pyproject.toml at repo root with:
   - [build-system] using setuptools
   - [project] section:
     - name = "claude-contract-negotiator"
     - version = "2.0.0"
     - description matching plugin.json description
     - requires-python = ">=3.11"
     - dependencies: list all runtime deps. Key ones:
       - "adeu @ git+https://github.com/dealfluence/adeu.git@v0.7.0" (NOT from PyPI -- only v0.0.1 placeholder there, we need v0.7.0)
       - python-docx, lxml, pydantic, mcp (check ~/Claude-Plugin source imports for the full list)
     - license = {text = "MIT"}
   - [tool.setuptools.packages.find] to discover src/ packages

   To find the full dependency list, check ~/Claude-Plugin for any requirements.txt, pyproject.toml, or scan imports in src/ files.

3. Rewrite scripts/start-mcp.sh:
   - Remove ALL hardcoded paths (/home/sarturko/...)
   - Auto-detect: SCRIPT_DIR from the script's own location, REPO_ROOT as parent of scripts/
   - Add Python version check at start: verify python3 is >= 3.11, exit with clear error if not
   - Use `python3` command (not hardcoded /usr/bin/python)
   - cd to REPO_ROOT, then exec python3 -m src.mcp_server "$@"
   - Keep PYTHONPATH only if needed (set to REPO_ROOT so src/ is importable)
   - Do NOT add pip install logic -- that's for the user to do per README instructions

4. Verify .mcp.json still works with the new start-mcp.sh (it uses ${CLAUDE_PLUGIN_ROOT}/scripts/start-mcp.sh which is correct).
  </action>
  <verify>
    <automated>test -f src/__init__.py && test -f src/mcp_server/__init__.py && test -f pyproject.toml && grep -q "adeu" pyproject.toml && grep -q "3.11" pyproject.toml && ! grep -q "/home/sarturko" scripts/start-mcp.sh && grep -q "python3" scripts/start-mcp.sh && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>src/ with 78 Python files bundled (no __pycache__), pyproject.toml with all deps including adeu git dep, start-mcp.sh auto-detects paths and checks Python 3.11+, no hardcoded personal paths anywhere</done>
</task>

</tasks>

<verification>
- `find claude-v-claude -type f 2>/dev/null | wc -l` returns 0
- `cat .claude-plugin/plugin.json | python3 -c "import sys,json; d=json.load(sys.stdin); assert 'Claude-Plugin-MCP' in d['repository']; assert 'privacy_policy' in d; print('OK')"`
- `grep -r "/home/sarturko" . --include="*.sh" --include="*.json" --include="*.toml" | grep -v .git | grep -v .planning` returns nothing
- `find src -name "*.py" -not -path "*__pycache__*" | wc -l` returns 78
- `grep "adeu.*git" pyproject.toml` shows the git dependency
</verification>

<success_criteria>
- No stale files (claude-v-claude/, settings.local.json) exist
- plugin.json correct (version 2.0.0, Claude-Plugin-MCP URL, privacy_policy)
- .gitignore excludes .planning/
- src/ bundled with all 78 Python files
- pyproject.toml pip-installable with adeu git dep
- start-mcp.sh has no hardcoded paths, checks Python 3.11+
- No secrets or personal paths in any file
</success_criteria>

<output>
After completion, create `.planning/phases/30-codebase-audit-and-documentation-review/30-01-SUMMARY.md`
</output>
