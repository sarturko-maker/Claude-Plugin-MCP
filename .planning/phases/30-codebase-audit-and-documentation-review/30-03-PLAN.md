---
phase: 30-codebase-audit-and-documentation-review
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [SYN-01, SYN-02, AUD-03]

must_haves:
  truths:
    - "yolo-negotiation command in ~/Claude-Plugin/commands/ matches this repo's commands/yolo-negotiation/"
    - "defaults/ directory in ~/Claude-Plugin/defaults/ matches this repo's defaults/"
    - "SKILL.md files are identical between this repo and ~/Claude-Plugin (Phase 29 anchoring fix present)"
  artifacts:
    - path: "commands/yolo-negotiation/"
      provides: "Yolo negotiation command source"
    - path: "defaults/"
      provides: "Default persona, authority, playbook files"
  key_links:
    - from: "commands/yolo-negotiation/"
      to: "~/Claude-Plugin/commands/yolo-negotiation/"
      via: "file sync (this repo -> source repo)"
      pattern: "identical content"
    - from: "defaults/"
      to: "~/Claude-Plugin/defaults/"
      via: "file sync (this repo -> source repo)"
      pattern: "identical content"
---

<objective>
Sync commands and defaults back to the source repo (~/Claude-Plugin) and verify SKILL.md files match between repos.

Purpose: The source repo (~/Claude-Plugin) must stay canonical. Any changes made in the packaged repo during v1.x phases need to flow back. SKILL.md verification ensures the Phase 29 anchoring fix is present.
Output: Both repos have identical commands/yolo-negotiation/, defaults/, and skills/ content.
</objective>

<execution_context>
@/home/sarturko/.claude/get-shit-done/workflows/execute-plan.md
@/home/sarturko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-codebase-audit-and-documentation-review/30-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync commands and defaults to source repo</name>
  <files>commands/yolo-negotiation/, defaults/</files>
  <action>
1. **Sync yolo-negotiation command** (SYN-01):
   - Compare this repo's commands/yolo-negotiation/ with ~/Claude-Plugin/commands/yolo-negotiation/ using diff -r
   - If differences exist, copy from THIS repo to ~/Claude-Plugin/commands/yolo-negotiation/ (this repo has the latest from v1.x packaging phases)
   - If identical, note "already in sync"

2. **Sync defaults/** (SYN-02):
   - Compare this repo's defaults/ with ~/Claude-Plugin/defaults/ using diff -r
   - If differences exist, copy from THIS repo to ~/Claude-Plugin/defaults/ (this repo has the latest)
   - If identical, note "already in sync"

3. **Verify SKILL.md** (AUD-03):
   - Compare this repo's skills/negotiate-contract/SKILL.md with ~/Claude-Plugin/skills/negotiate-contract/SKILL.md using diff
   - Verify Phase 29 anchoring fix is present in both: look for anchoring-related content, commenting rules, validation gate, input validation mentions
   - If they differ, determine which is canonical (source repo ~/Claude-Plugin should be canonical for skills) and copy FROM source TO this packaged repo
   - Report exact diff if any differences found

Note: Do NOT sync docs back (per user decision -- docs live separately in each repo). Only commands/ and defaults/.
  </action>
  <verify>
    <automated>diff -r commands/yolo-negotiation/ ~/Claude-Plugin/commands/yolo-negotiation/ > /dev/null 2>&1 && diff -r defaults/ ~/Claude-Plugin/defaults/ > /dev/null 2>&1 && diff skills/negotiate-contract/SKILL.md ~/Claude-Plugin/skills/negotiate-contract/SKILL.md > /dev/null 2>&1 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>commands/yolo-negotiation/ identical in both repos, defaults/ identical in both repos, SKILL.md identical with Phase 29 anchoring fix confirmed present</done>
</task>

</tasks>

<verification>
- `diff -r commands/yolo-negotiation/ ~/Claude-Plugin/commands/yolo-negotiation/` shows no differences
- `diff -r defaults/ ~/Claude-Plugin/defaults/` shows no differences
- `diff skills/negotiate-contract/SKILL.md ~/Claude-Plugin/skills/negotiate-contract/SKILL.md` shows no differences
- `grep -i "anchor\|validation\|commenting" skills/negotiate-contract/SKILL.md` confirms Phase 29 content
</verification>

<success_criteria>
- yolo-negotiation command synced: both repos identical
- defaults/ synced: both repos identical
- SKILL.md verified: identical between repos, Phase 29 anchoring fix present
</success_criteria>

<output>
After completion, create `.planning/phases/30-codebase-audit-and-documentation-review/30-03-SUMMARY.md`
</output>
